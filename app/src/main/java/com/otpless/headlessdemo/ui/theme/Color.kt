package com.otpless.headlessdemo.ui.theme

import androidx.compose.ui.graphics.Color

val OtplessBlack = Color(0xFF070707)
val OtplessWhite = Color(0xFFFFFFFF)

// Neutral grays derived from the black/white brand pair — used only for
// secondary surfaces, outlines and the "busy" status dot so those states stay
// visually distinct without introducing an unrelated accent hue.
val OtplessGraySurface = Color(0xFFF2F2F2)
val OtplessGrayOutline = Color(0xFF9E9E9E)
val OtplessGrayMid = Color(0xFF757575)

